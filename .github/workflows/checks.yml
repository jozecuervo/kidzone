name: Checks

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  kidzone:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 24.x
      - run: node ./scripts/check.mjs
