{
  "name": "kidzone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node ./scripts/check.mjs",
    "test": "playwright test",
    "snapshots": "playwright test tests/project-snapshots.spec.mjs",
    "snapshots:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
